package examples.f_objects;

// this has no main method so there is nothing here to run
// it's method must be called from another class
public class SimpleObject {

    public int add(int one, int two) {
        return one + two;       
    }
}
